<%= form_with(model: survey, local: true) do |form| %>
  <% if survey.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(survey.errors.count, "error") %> prohibited this survey from being saved:</h2>

      <ul>
      <% survey.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= form.label :name %>
    <%= form.text_area :name, class: "form-control" %>
  </div>

  <div class="form-group">
    <%= form.label :laptop, "Do you own a laptop?" %><br />
    <%= form.label :laptop, "Yes", :value => "true"  %>
    <%= form.radio_button :laptop, 'Yes' %>

    <%= form.label :laptop, "No", :value => "false"  %>
    <%= form.radio_button :laptop, 'No', :checked => true %>


  </div>

  <div class="form-group">
    <%= form.label :os, "What operating system do you use?" %><br />
    <%= form.select :os, ['I don\'t have a laptop', 'Mac OS', 'Windows', 'Linux'] %>
  </div>

  <div class="form-group">
    <%= form.submit "Submit", class: "btn btn-primary" %>
  </div>
<% end %>
